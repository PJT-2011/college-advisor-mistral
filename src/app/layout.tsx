import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { Providers } from "./providers";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "College Advisor AI - Your Personal Campus Guide",
  description: "Get personalized academic guidance, wellness support, and campus life recommendations powered by local AI.",
  keywords: ["college advisor", "AI tutor", "campus life", "student wellness", "academic support"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
